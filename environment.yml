name: lmas-trgc
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pytorch
  - pytorch-cuda=12.1
  - pandas
  - numpy
  - networkx
  - pyyaml
  - tqdm
  - rich
  - pytest
  - matplotlib
  - pip:
      - openai
      - httpx
      - pydantic
      - pydantic-settings
      - python-dotenv
      - orjson
      - datasets
      - jinja2
      - tiktoken
      - typing_extensions
